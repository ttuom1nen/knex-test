knex init command creates knexfile.ts

Migrations:
Migrations are used to define sets of schema changes for updating the database.
requires ts-node package
Created into ./data/migrations/

knex migrate:make todos
knex migrate:latest = npm run migrate
knex migrate:rollback = npm run unmigrate

Seeds:
Seed files allow you to populate your database with test or seed data independent of your migration files.
Created into ./data/seeds/

knex seed:make todos
knex seed:run = npm run seed